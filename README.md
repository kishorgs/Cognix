# Cognix
A full-stack RAG system that converts uploaded documents into vector embeddings stored in FAISS. User queries are embedded and matched via semantic search to retrieve relevant chunks. Retrieved context augments LLM prompts, generating accurate, cited responses. Built with React, Node.js, FastAPI, MongoDB, and Firebase.
